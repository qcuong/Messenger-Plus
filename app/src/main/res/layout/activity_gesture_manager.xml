<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.quoccuong.messenger.GestureManagerActivity">

    <ListView
        android:id="@+id/activity_gesture_manager_lv_lisgusture"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentBottom="true"></ListView>

    <ImageView
        android:id="@+id/activity_gesture_manager_imv_newsms"
        android:layout_width="58dp"
        android:layout_height="58dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="32dp"
        android:layout_marginEnd="32dp"
        android:layout_marginRight="32dp"
        android:background="@drawable/circle"
        android:padding="17dp"
        android:elevation="5dp"
        android:tint="@android:color/white"
        android:src="@drawable/plus" />

</RelativeLayout>
